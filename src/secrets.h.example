#pragma once

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";
// Mac's Bonjour/mDNS local hostname (System Settings > General > Sharing >
// Local hostname), WITHOUT the ".local" suffix. Find it with:
//   scutil --get LocalHostName
const char* backend_mdns_host = "YOUR_MAC_HOSTNAME";
// Used only if mDNS resolution fails (some networks block mDNS multicast).
const char* backend_fallback_ip = "YOUR_DESKTOP_IP";
const int backend_port = 5000;
